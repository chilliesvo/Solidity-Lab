const { ONE_DAY, TEN_MINUTES } = require("../utils/utils");

describe("Test", () => {
    before(async () => {
    });

    describe("Compare Gas Batch Mint", () => {
        it("Get TimeStamp", async () => {
            const currentTimestamp = Date.now();
            const currentTimestamp2 = new Date().getTime();
            console.log('currentTimestamp :>> ', currentTimestamp);
            console.log('currentTimestamp2 :>> ', currentTimestamp2);
        });
    });
});
